# Job DB 接続

## 目的

Insight と **Job** エンティティを双方向に辿れるようにする。

## 接続方法（プレースホルダ）

- Notion: Relation プロパティ名 `（要設定）` ↔ Job DB の `（要設定）`
- 同期バッチ: `（要設定）`

## 注意

- Job 側がマスタのフィールドは Job を正とし、Insight 側はコピーを持たない。
