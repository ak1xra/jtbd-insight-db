# insight DB Specification

**Version**: 2.0
**Status**: Active
**Last Updated**: 2026-05-03
**Owner**: AK1RA

---

## 1. 概要

### 1.1 目的

JTBDベース マーケティング戦略立案のための顧客インサイト管理データベースの仕様を定義する。

### 1.2 スコープ

- Notion DB のプロパティ設計
- AI autofill プロンプト仕様
- 他DBとのRelation設計
- 入力・レビュー・運用ルール

### 1.3 対象読者

- DB の入力者・レビュアー
- 戦略立案担当者
- システム保守担当者

---

## 2. システム要件

### 2.1 プラットフォーム

| 項目 | 要件 |
|---|---|
| プラットフォーム | Notion |
| プラン | Plus プラン以上（AI autofill 利用条件） |
| AI モデル | Notion AI Opus 4.6（autofill）/ Sonnet 4.6（汎用処理） |

### 2.2 接続先DB

| DB名 | 論理名 | 必須/任意 | 用途 | スキーマ |
|---|---|---|---|---|
| `Insight` | `insight` | （本DB） | 顧客の生の声を管理 | [schemas/insight-db-schema.md](./schemas/insight-db-schema.md) |
| `Job` | `job` | 必須 | insight が紐づく Job | [schemas/job-db-schema.md](./schemas/job-db-schema.md) |
| `TagDictionary` | `tag_dictionary` | 必須 | 横断的タグ管理 | [schemas/property-definitions.md](./schemas/property-definitions.md) |
| `VPC` | `vpc` | 推奨 | バリュープロポジションキャンバス | [schemas/vpc-db-schema.md](./schemas/vpc-db-schema.md) |
| `BMC` | `bmc` | 推奨 | ビジネスモデルキャンバス | [schemas/bmc-db-schema.md](./schemas/bmc-db-schema.md) |

---

## 3. プロパティ仕様

### 3.1 完全なプロパティ一覧

| # | プロパティ名 | 型 | 必須 | AI autofill | 説明 |
|---|---|---|---|---|---|
| 1 | `insight名` | Title | ✅ | ✅ | insightの見出し（25文字以内） |
| 2 | `生の声` | Text | ✅ | ❌ | 発言・観察・メモ原文（一次情報） |
| 3 | `情報源タイプ` | Select | ✅ | ❌ | 情報の取得元 |
| 4 | `取得日` | Date | ✅ | ❌ | 情報取得日 |
| 5 | `顧客属性` | Select | ✅ | ❌ | 購買・利用プロセス上の役割 |
| 6 | `役職・立場` | Text | ❌ | ❌ | 組織上の肩書き |
| 7 | `所属部門` | Text | ❌ | ❌ | 例: マーケ、営業、人事 |
| 8 | `Forces` | Multi-select | ✅* | ✅ | JTBD Forces of Progress |
| 9 | `ジャーニー段階` | Select | ✅ | ✅ | 購買・意思決定プロセス段階 |
| 10 | `重要度` | Number | ✅ | ❌ | 1-5評価（人手） |
| 11 | `確からしさ` | Number | ✅ | ❌ | 1-5評価（人手） |
| 12 | `要約` | Text | ✅ | ✅ | Forces構造化要約 |
| 13 | `job` | Relation | ✅** | ❌ | `job` DBへの接続 |
| 14 | `タグ` | Relation | ❌ | ❌ | `TagDictionary` DBへの接続 |
| 15 | `レビュー状態` | Select | ✅ | ❌ | 下書き / レビュー中 / 確定 |
| 16 | `レビュー者` | Person | ❌ | ❌ | レビュー担当者 |
| 17 | `レビュー日` | Date | ❌ | ❌ | レビュー完了日 |

- **`*`（`Forces`）**: 「条件付き必須」。`レビュー状態 = 確定` までに **(A) 最低 1 つ選択** するか、**(B) 空欄の理由を `要約` の【主訴】に明記**する。AI autofill 直後の空欄は許容。詳細は [`property-definitions.md`](./schemas/property-definitions.md#8-forces) §8。
- **`**`（`job`）**: 「段階的必須」。作成直後〜 AI autofill 中は未接続でもよいが、`レビュー状態 = 下書き` に進む前に接続必須。詳細は [`relations.md`](./schemas/relations.md#1-insightjob--job-db) §1。

**MD形式の分割スキーマ**（同一内容の参照用・自動化の入力元）: [`schemas/insight-db-schema.md`](./schemas/insight-db-schema.md) · [`property-definitions.md`](./schemas/property-definitions.md) · [`select-options.md`](./schemas/select-options.md) · [`relations.md`](./schemas/relations.md)

### 3.2 Select / Multi-select 候補

#### `情報源タイプ`（Select）

| 値 | 説明 |
|---|---|
| `interview` | インタビュー |
| `survey` | アンケート |
| `sales_call` | 営業電話・商談 |
| `support` | サポート問い合わせ |
| `sns` | SNS投稿 |
| `review` | レビュー記事・口コミ |
| `analytics` | 行動データ |

#### `顧客属性`（Select）

| 値 | 定義 |
|---|---|
| `意思決定者` | 最終決裁権を持つ者 |
| `決裁関与者` | 決裁プロセスに影響を与える者 |
| `実務担当者` | 業務遂行を担う者 |
| `利用者` | サービス・製品の実際の使い手 |
| `導入推進者` | 導入を主導する者 |
| `既存顧客` | 既に契約・利用中の顧客 |
| `見込み顧客` | 未契約だが購買検討段階の対象 |
| `代理店・パートナー` | 販売・導入支援を行う第三者 |
| `不明` | 判定材料が不足 |

#### `Forces`（Multi-select）

| 値 | 定義 | 例 |
|---|---|---|
| `Push` | 現状から押し出す力 | 「今のシステムが遅すぎて業務にならない」 |
| `Pull` | 新しい解決策へ引き寄せる力 | 「自動化で残業を月20時間減らしたい」 |
| `Anxiety` | 変化への不安・懸念 | 「導入失敗したら社内で責められる」 |
| `Habit` | 現状維持を促す力 | 「今のやり方で20年回してきた」 |

#### `ジャーニー段階`（Select）

| 値 | 定義 |
|---|---|
| `認知` | 課題や選択肢をまだ広く知り始めた段階 |
| `興味` | 課題意識があり、関連情報に関心を持ち始めた段階 |
| `比較` | 複数案や複数サービスを見比べている段階 |
| `検討` | 導入候補が絞られ、具体条件を詰めている段階 |
| `導入` | 契約・実装・利用開始の段階 |
| `継続` | 利用後の定着・改善・更新・活用拡大の段階 |
| `離脱・見送り` | 導入しない・解約する・保留する理由が中心の段階 |
| `不明` | 判定材料が不足している場合 |

#### `レビュー状態`（Select）

| 値 | 定義 |
|---|---|
| `下書き` | AI autofill完了、レビュー未着手 |
| `レビュー中` | レビュー実施中 |
| `確定` | レビュー完了、戦略立案に使用可 |

### 3.3 Number プロパティ仕様

#### `重要度`（1-5）

| 値 | 定義 |
|---|---|
| 5 | 戦略の根幹に関わる |
| 4 | 重要な意思決定材料 |
| 3 | 参考情報として有用 |
| 2 | 補助的情報 |
| 1 | ノイズに近い |

#### `確からしさ`（1-5）

| 値 | 定義 |
|---|---|
| 5 | 一次情報（録音・記録あり） |
| 4 | 信頼できる情報源からの伝聞 |
| 3 | 複数の傍証あり |
| 2 | 単一情報源・確認困難 |
| 1 | 推測・解釈に依存 |

---

## 4. AI Autofill 仕様

### 4.1 実行順序

```
1. insight名（生の声入力後すぐ）
2. Forces（生の声 + 顧客属性が揃った後）
3. ジャーニー段階（Forces 確定後）
4. 要約（全項目確定後）
```

### 4.2 共通方針

- **モデル**: Notion AI Opus 4.6
- **一次情報**: `生の声` のみ
- **ハルシネーション抑止**: 明示されていない情報は推測しない
- **出力形式**: プロンプト指定フォーマット厳守

### 4.3 各プロンプトの詳細仕様

各プロンプトの詳細は以下を参照：
- [insight名](./docs/ai-prompts/insight-name.md)
- [Forces](./docs/ai-prompts/forces.md)
- [ジャーニー段階](./docs/ai-prompts/journey-stage.md)
- [要約](./docs/ai-prompts/summary.md)

---

## 5. Relation 設計

### 5.1 DB間関係図

```
┌──────────────────────┐
│      insight DB      │
└──────────┬───────────┘
           │
           ├─── job (Relation)
           │      │
           │      └─── VPC (Relation)
           │            │
           │            └─── BMC (Relation)
           │
           └─── タグ (Relation)
                  │
                  └─── TagDictionary
```

### 5.2 各Relation仕様

#### `insight.job` → `job DB`

| 項目 | 仕様 |
|---|---|
| 型 | Relation |
| 多重度 | 多対1（1つのinsightは1つのjobに紐づく） |
| 必須 | ✅**（`レビュー状態 = 下書き` へ至るまでに接続。作成直後は空可。詳細は [`relations.md`](./schemas/relations.md#1-insightjob--job-db)） |
| 双方向 | ✅（job側に `関連insight` プロパティ） |

#### `insight.タグ` → `TagDictionary`

| 項目 | 仕様 |
|---|---|
| 型 | Relation |
| 多重度 | 多対多 |
| 必須 | ❌ |
| 双方向 | ✅ |

---

## 6. データ品質基準

### 6.1 必須性のレベル定義（Human-in-the-Point）

各プロパティの「必須」は、**Notion UI の技術的必須**ではなく、**運用ゲート**として定義する。判断は人間が行う。

#### Step 2（insight 作成直後）

- [ ] `生の声` が記入されている

#### Step 7〜8（`レビュー状態 = 下書き` にする前）

- [ ] Step 2 の必須項目を満たしている
- [ ] `情報源タイプ` `取得日` `顧客属性` が設定されている
- [ ] `重要度` `確からしさ` が人手で評価されている
- [ ] `insight名` `Forces` `ジャーニー段階` `要約` の AI autofill が完了している（内容の承認はこの後のレビューで行う）
- [ ] `job` Relation が接続されている（未分類なら `unclassified_job` への仮接続でもよい）

#### `レビュー状態 = 確定` にする前（レビュー完了ゲート）

- [ ] Step 7〜8 の必須項目を満たしている
- [ ] `Forces` を人がレビュー済み  
  - [ ] **最低 1 つ選択されている**、または **空欄の理由が `要約` の【主訴】に明記**されている
- [ ] `ジャーニー段階` を人がレビュー済み
- [ ] `要約` を人がレビュー済み（`Forces` が空欄の場合、【主訴】の空欄理由が読み取れること）
- [ ] AI autofill のハルシネーションがないことを確認
- [ ] `レビュー者` `レビュー日` が記入されている

### 6.2 戦略立案使用条件

戦略立案・VPC/BMC連携対象とするための条件：

- `レビュー状態 = 確定`
- `重要度 ≥ 4`
- `確からしさ ≥ 4`

### 6.3 品質メトリクス

| メトリクス | 目標値 | 計測方法 |
|---|---|---|
| ハルシネーション率 | < 10% | 月次サンプル検証 |
| `不明` 比率（ジャーニー段階） | < 20% | 全件集計 |
| `重要度4以上` 比率 | 30〜50% | 全件集計 |
| 入力からレビュー完了までの平均日数 | < 7日 | レビュー日 - 取得日 |

---

## 7. 運用ルール

### 7.1 入力フロー

```
[1] 一次情報入手
       ↓
[2] insight DB に新規エントリ作成
       ↓
[3] 生の声を貼り付け
       ↓
[4] 情報源タイプ・取得日・顧客属性を設定
       ↓
[5] AI autofill 実行（4項目）
       ↓
[6] 重要度・確からしさを人手評価
       ↓
[7] job・タグを Relation 接続
       ↓
[8] レビュー状態 = 下書き
```

**Human-in-the-Point（このフローの読み方）**

- **Step 5（AI autofill）**: AI は候補を出すだけ。ここでは確定しない。
- **Step 6（重要度・確からしさ）**: 戦略文脈に依存するため、**必ず人が**付ける。
- **Step 7（job・タグ）**: AI 出力と一次情報を踏まえて、**人が**紐づけ先を判断する（作成直後から Step 6 までの間、`job` が空でもよい）。

### 7.2 レビューフロー

```
[1] レビュー状態 = レビュー中 に変更
       ↓
[2] AI autofill 出力の妥当性確認
       ↓
[3] ハルシネーション検出
       ↓
[4] 必要なら修正
       ↓
[5] レビュー者・レビュー日を記入
       ↓
[6] レビュー状態 = 確定
```

### 7.3 メンテナンスサイクル

| タイミング | 作業 |
|---|---|
| 20-30件累積時 | `顧客属性` 分類見直し |
| 50件累積時 | `Forces` ラベル判定精度検証 |
| 月次 | ハルシネーション率サンプル検証 |
| 四半期 | プロンプト最適化レビュー |

### 7.4 推奨ビュー（Notion）

> **実装方法**: Notion API は DB ビューの新規作成に対応していないため、本節は **Notion UI で手動作成するための仕様**として位置づける。各ビューの **名称・型・フィルタ・ソート・グループ・表示プロパティ** は本節を正本とする。
>
> **デフォルトビュー**: 全ロール（入力者 / レビュアー / 戦略立案者）の **デフォルトは `5. 戦略候補`** とする。各ロール側の使用頻度に応じて、後日 AK1RA がロール別デフォルトを再設定してよい。

#### Tier 1: Core ビュー（必須・初期構築時に作成）

| # | ビュー名 | 型 | フィルタ | ソート | 表示プロパティ | 用途 |
|---|---|---|---|---|---|---|
| 1 | `All Insights` | Table | なし | `取得日` 降順 | `insight名` / `顧客属性` / `Forces` / `ジャーニー段階` / `重要度` / `確からしさ` / `レビュー状態` / `取得日` | 全件参照 |
| 2 | `下書き` | Table | `レビュー状態 = 下書き` | `取得日` 降順 | Tier1 共通 | レビュー待ち一覧 |
| 3 | `レビュー中` | Table | `レビュー状態 = レビュー中` | `取得日` 降順 | Tier1 共通 | 進行中のレビュー |
| 4 | `確定済み` | Table | `レビュー状態 = 確定` | `重要度` 降順 | Tier1 共通 | 戦略立案候補プール |
| 5 | `戦略候補` ★ | Table | `レビュー状態 = 確定` AND `重要度 ≥ 4` AND `確からしさ ≥ 4` | `重要度` 降順 | Tier1 共通 + `要約` | **VPC / BMC 連携対象（最重要・全ロールのデフォルト）** |

#### Tier 2: 分析ビュー（データ 20 件以降に作成）

| # | ビュー名 | 型 | グループ | フィルタ | 用途 |
|---|---|---|---|---|---|
| 6 | `Forces 分析` | Board | `Forces` | `レビュー状態 = 確定` | Push / Pull / Anxiety / Habit の分布把握 |
| 7 | `ジャーニー分析` | Board | `ジャーニー段階` | `レビュー状態 = 確定` | ファネル分析（認知〜継続） |
| 8 | `顧客属性分析` | Board | `顧客属性` | `レビュー状態 = 確定` | セグメント別分布の把握 |

- **共通カード表示プロパティ**: `insight名` / `重要度` / `確からしさ` / `取得日`
- **`Forces 分析`** は Multi-select のため、1 件が複数カラムに重複表示される点に留意する

#### Tier 3: 品質管理ビュー（データ 50 件以降に作成）

| # | ビュー名 | 型 | フィルタ | 用途 |
|---|---|---|---|---|
| 9 | `⚠️ 未接続 insight` | Table | `job` が空 AND `レビュー状態 ≠ 下書き` | `job` 未接続の検出（[`relations.md` §1](./schemas/relations.md#1-insightjob--job-db) と整合） |
| 10 | `⚠️ Forces 空欄` | Table | `Forces` が空 AND `レビュー状態 = 確定` | 空欄理由レビュー対象（[`property-definitions.md` §8](./schemas/property-definitions.md#8-forces) の (B) 条件確認） |
| 11 | `⚠️ 古い下書き` | Table | `レビュー状態 = 下書き` AND `取得日 < 7 日前` | レビュー遅延検出（§6.3 の品質メトリクスと整合） |
| 12 | `⚠️ 要約未生成` | Table | `要約` が空 | AI autofill 漏れ検出 |

- **共通表示プロパティ**: `insight名` / `レビュー状態` / `取得日` / `レビュー者`
- 品質管理ビューはアラート用途のため、件数が多い場合はメンテナンスサイクル（§7.3）で対処する

#### ビュー設計の原則

1. **`レビュー状態` を主要フィルタ軸**にする（Human-in-the-Point の可視化）
2. **戦略立案で使うビュー（`戦略候補`）は条件を厳格に**保ち、未確定 insight が混入しないようにする
3. **品質管理ビューは「⚠️」プレフィックス**で識別し、健全運用時は空であることが望ましい
4. **ロール別デフォルトを変更する場合**でも、`戦略候補` の存在と条件は変更しない（戦略の根拠データの基準として固定）

---

## 8. Forbidden Actions（禁止事項）

| ⚠️ | 禁止事項 |
|---|---|
| 1 | AI autofill の出力を人手レビューなしに確定する |
| 2 | `生の声` に明示されていない情報を AI に推測させる |
| 3 | `重要度` `確からしさ` を AI に判定させる |
| 4 | `レビュー状態 = 下書き` のinsightを戦略立案に使用する |
| 5 | 移行作業中（v1.3→v2.0）に本番運用を継続する |
| 6 | 旧プロパティの削除を移行Step 4完了前に実施する |
| 7 | `生の声` を編集・改変する（補足はコメントで） |

---

## 9. セキュリティ・プライバシー

### 9.1 機密情報の取り扱い

- 顧客個人を特定可能な情報（実名・連絡先等）は `生の声` に含めない
- 必要な場合はマスキング（例: A社、B氏）
- 機密度高の情報は別途アクセス制限DB へ分離

### 9.2 アクセス制御

| ロール | 権限 |
|---|---|
| Owner（AK1RA） | フルアクセス |
| Reviewer | 編集 + レビュー状態変更 |
| Contributor | 新規作成・自分のエントリ編集のみ |
| Viewer | 確定済みのみ閲覧 |

---

## 10. バージョン管理

### 10.1 セマンティックバージョニング

`MAJOR.MINOR` 形式：
- **MAJOR**: 破壊的変更（プロパティ削除・型変更）
- **MINOR**: 機能追加・プロンプト改善

### 10.2 変更履歴

[CHANGELOG.md](./CHANGELOG.md) を参照。

---

## 11. 用語集

| 用語 | 定義 |
|---|---|
| JTBD | Jobs to be Done。顧客が片付けたい用事の理論 |
| Forces of Progress | JTBDの4つの力（Push/Pull/Anxiety/Habit） |
| VPC | Value Proposition Canvas |
| BMC | Business Model Canvas |
| Human-in-the-Point | AI出力の確定は必ず人間が判断する設計原則 |
| SSOT | Single Source of Truth |
| ハルシネーション | AIが原文にない情報を生成する現象 |
| `レビュー状態` | Notion Select。`下書き` / `レビュー中` / `確定`。文書中の「確定」は原則として本プロパティ値を指す |

---

## 12. 参考資料

- Clayton Christensen "Competing Against Luck"（JTBD理論）
- Bob Moesta "Demand-Side Sales 101"（Forces of Progress）
- Alexander Osterwalder "Value Proposition Design"（VPC）
- Alexander Osterwalder "Business Model Generation"（BMC）

---

## 13. 改訂履歴

| Version | Date | Author | Changes |
|---|---|---|---|
| 2.0 | 2026-05-03 | AK1RA | Forces ラベル化、テキストフィールド統合 |
| 1.3 | 2026-05-03 | AK1RA | 顧客属性の仮分類追加 |
| 1.0 | - | AK1RA | 初版 |